<!DOCTYPE html>
<html>
  <head>
    <title>TheGossipProject</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!-- Custom styles for this template -->
    <link href="https://getbootstrap.com/docs/3.3/examples/cover/cover.css" rel="stylesheet">
    <!-- le font Montserrat de google -->
<!--link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet" -->

  </head>

  <body>
<nav class="navbar navbar-expand-lg text-primary navbar-dark bg-dark ">
  <%= link_to "The Gossip Project", root_path, :class => "navbar-brand text-primary" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to "Home", root_path, :class => "nav-link" %>
      </li>
      <li class="nav-item active">
        <%= link_to "L-Quipe", team_path, :class => "nav-link" %>
      </li>
      <li class="nav-item active">
        <%= link_to "Ring My Bell", contact_path, :class => "nav-link" %>
      </li>
      <li class="nav-item active">
         <%= link_to 'Créer un Gossip', new_gossip_path, :class =>"nav-link" %>
      </li>
      <% if logged_in? %>
        <li class="nav-item active">
          <%= link_to "Se déconnecter", session_path(session[:user_id]), method: :delete, :class =>"nav-link" %>
        </li>
      <% else %>
        <li class="nav-item active">
          <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
             Sign In | Sign Up
            </button>
            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
              <%= link_to 'Créer son compte', new_user_path, :class =>"nav-link text-dark" %>
              <%= link_to 'Se connecter', new_session_path, :class =>"nav-link text-dark" %>
            </div>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

    <!--div class="container">
    </div>< /.container -->
    <div class="container text-primary">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
